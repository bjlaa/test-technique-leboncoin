import React from 'react';

describe('MessageForm', () => {
	it('should match its snapshot', () => {
		expect(false).toBe(true);
	});

	it('should create a new message on form submit', () => {
		expect(false).toBe(true);
	});

	it('should empty the form textarea upon form submit', () => {
		expect(false).toBe(true);
	});
});
